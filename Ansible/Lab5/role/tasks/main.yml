---
# tasks file for role
- name: install httpd
  yum:
    name: httpd
    state: present
- name: start httpd
  service:
    name: httpd
    state: started
    enabled: true
- name: copy index.html
  copy:
    src: ../files/index.html
    dest: /var/www/html/index.html
  notify: restart httpd
